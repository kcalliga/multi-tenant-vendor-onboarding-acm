apiVersion: policy.open-cluster-management.io/v1
kind: Policy
metadata:
  name: policy-onboard-generic-vendor # <-- SEARCH & REPLACE HERE
  namespace: open-cluster-management-policies
  annotations:
    policy.open-cluster-management.io/description: "Onboard Workspace for generic-vendor" # <-- SEARCH & REPLACE HERE
spec:
  disabled: false # Ready to fire immediately when you save it as a new policy
  remediationAction: enforce
  policy-templates:
    - objectDefinition:
        apiVersion: policy.open-cluster-management.io/v1
        kind: ConfigurationPolicy
        metadata:
          name: config-generic-vendor-env # <-- SEARCH & REPLACE HERE
        spec:
          remediationAction: enforce
          severity: high
          object-templates:

            # 1. GENERATE THE ISOLATED NAMESPACE
            - complianceType: musthave
              objectDefinition:
                apiVersion: v1
                kind: Namespace
                metadata:
                  name: generic-vendor-apps # <-- SEARCH & REPLACE HERE

            # 2. ENFORCE RESTRICTED RBAC
            - complianceType: musthave
              objectDefinition:
                apiVersion: rbac.authorization.k8s.io/v1
                kind: RoleBinding
                metadata:
                  name: generic-vendor-admin-binding # <-- SEARCH & REPLACE HERE
                  namespace: generic-vendor-apps # <-- SEARCH & REPLACE HERE
                subjects:
                - kind: User
                  name: generic-vendor-user # <-- SEARCH & REPLACE HERE
                  apiGroup: rbac.authorization.k8s.io
                roleRef:
                  kind: ClusterRole
                  name: admin
                  apiGroup: rbac.authorization.k8s.io

            # 3. INJECT THE REGISTRY LINK INTO THE NAVIGATION SIDEBAR
            - complianceType: musthave
              objectDefinition:
                apiVersion: console.openshift.io/v1
                kind: ConsoleLink
                metadata:
                  name: generic-vendor-registry-link # <-- SEARCH & REPLACE HERE
                spec:
                  href: "https://quay-server.apps.platform-customer.com/generic-vendor" # <-- SEARCH & REPLACE HERE
                  location: NavigationMenu
                  text: "generic-vendor Registry" # <-- SEARCH & REPLACE HERE
                  navigationMenu:
                    section: Vendor Tools

            # 4. REMOVE LOCAL OCP CLUSTER PERSPECTIVES FROM THE VENDOR
            - complianceType: musthave
              objectDefinition:
                apiVersion: operator.openshift.io/v1
                kind: Console
                metadata:
                  name: cluster
                spec:
                  customization:
                    perspectives:
                      - id: admin
                        visibility:
                          state: AccessReview
                          accessReview:
                            missing:
                              resource: clusterrolebindings
                              verb: list
                      - id: dev
                        visibility:
                          state: AccessReview
                          accessReview:
                            missing:
                              resource: clusterrolebindings
                              verb: list
---
apiVersion: policy.open-cluster-management.io/v1
kind: PlacementBinding
metadata:
  name: binding-onboard-generic-vendor # <-- SEARCH & REPLACE HERE
  namespace: open-cluster-management-policies
placementRef:
  name: placement-onboard-generic-vendor # <-- SEARCH & REPLACE HERE
  apiGroup: cluster.open-cluster-management.io
  kind: Placement
subjects:
- name: policy-onboard-generic-vendor # <-- SEARCH & REPLACE HERE
  apiGroup: policy.open-cluster-management.io
  kind: Policy
---
apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  name: placement-onboard-generic-vendor # <-- SEARCH & REPLACE HERE
  namespace: open-cluster-management-policies
spec:
  predicates:
    - requiredClusterSelector:
        labelSelector:
          matchExpressions:
            - {key: local-cluster, operator: In, values: ["true"]}
